# ~/.tmuxinator/search-edit.yml

name: search-edit
root: ~/Develop/Work/IKEA/search-edit

windows:
  - editor: $EDITOR
  - test:
      layout: tiled
      panes:
        - cd .. && ./cloud_sql_proxy -instances='ingka-rrm-search-dev:europe-west1:gena=tcp:5432'
        - pwd
        - npm run dev
        - npm run server
